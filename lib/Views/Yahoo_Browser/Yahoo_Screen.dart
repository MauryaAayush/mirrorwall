import 'package:flutter/material.dart';

class YahooScrenn extends StatelessWidget {
  const YahooScrenn({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(

      ),
      body: Container(),
    );
  }
}
